/** Re-export shared bot RPC from `@stonegy/helper` (stable import path for server). */
export {
  handleSessionCommand,
  type SessionCommandHost,
  type SessionCommandResponse,
} from "@stonegy/helper/core/session-commands";
